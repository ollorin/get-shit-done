---
phase: 47-telegram-reliability
plan: 03
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - agents/gsd-phase-coordinator.md
  - .planning/config.json
  - get-shit-done/bin/gsd-tools.test.js
autonomous: true
requirements: [MILE-21]
must_haves:
  truths:
    - "A blocking Telegram escalation that times out or fails because the daemon is down during autonomous execution resolves to a defined fallback (DEFERRED.json waiver, approver: timeout-fallback) instead of hanging the phase run indefinitely"
    - "The escalation timeout is configurable via .planning/config.json instead of hardcoded, with a safe default when the key is absent"
    - "The one and only ask_blocking_question call site (agents/gsd-phase-coordinator.md) is wrapped with fallback handling; workflows/execute-roadmap.md is confirmed (fresh grep) to have no blocking call site needing the same treatment"
    - "The exact deferred-waiver mechanism the fallback prose invokes (gsd-tools.js's deferred add CLI) is proven end-to-end by a real test: an approver: timeout-fallback entry is written and surfaced by deferred list"
  artifacts:
    - path: agents/gsd-phase-coordinator.md
      provides: "Step A of the sensitive-item escalation loop wrapped with fallback handling on ask_blocking_question failure"
    - path: .planning/config.json
      provides: "telegram.escalation_timeout_minutes configurable key (default 30) read by the coordinator prompt instead of a hardcoded literal"
    - path: get-shit-done/bin/gsd-tools.test.js
      provides: "A new describe block proving deferred add --approver timeout-fallback + deferred list round-trip"
  key_links:
    - from: agents/gsd-phase-coordinator.md
      to: get-shit-done/bin/gsd-tools.js
      via: "on ask_blocking_question failure, coordinator runs `node get-shit-done/bin/gsd-tools.js deferred add {phase} --step discuss --approver timeout-fallback --reason \"...\"`"
      pattern: "deferred add.*timeout-fallback"
e2e_flows: []
---

<objective>
Close MILE-21's success criterion #3: a blocking Telegram question that goes unanswered overnight (timeout) or fails because the daemon is down at escalation time must resolve to a defined fallback instead of hanging the phase run forever. `agents/gsd-phase-coordinator.md`'s sensitive-item escalation loop is the ONLY `ask_blocking_question` call site in `agents/`, `workflows/`, or `commands/` (verified by grep on 2026-07-04, re-verify fresh during execution) -- `workflows/execute-roadmap.md` has no blocking call site, only fire-and-forget notifications, so there is nothing to wire there.

Purpose: without this, a single unanswered escalation permanently blocks an unattended roadmap run -- exactly the failure mode MILE-21 exists to close.

Output: Step A of the coordinator's escalation loop is wrapped with fallback handling (log loudly, write a `DEFERRED.json` waiver via the existing sanctioned `deferred add` CLI with `approver: timeout-fallback`, park the item into Claude's Discretion, continue); the escalation timeout is configurable via `.planning/config.json`; a real test proves the exact CLI invocation the prose will run actually writes and surfaces the waiver.
</objective>

<execution_context>
@/Users/ollorin/get-shit-done/get-shit-done/workflows/execute-plan.md
@/Users/ollorin/get-shit-done/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-telegram-reliability/47-CONTEXT.md
@.planning/STATE.md
@.planning/config.json

@agents/gsd-phase-coordinator.md
</context>

<operational_constraint>
This plan does not touch `mcp-servers/telegram-mcp/**` at all -- no daemon/adapter restart concerns apply. The only "live" concern is that `agents/gsd-phase-coordinator.md` is the exact prompt file governing the currently-running roadmap coordinator; edits here take effect on the NEXT phase-coordinator invocation (a fresh agent spawn), not retroactively on any in-flight coordinator call. Do not attempt to interrupt or reload the currently-running coordinator.
</operational_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Configurable escalation timeout + fallback wiring in gsd-phase-coordinator.md's Step A</name>
  <files>agents/gsd-phase-coordinator.md, .planning/config.json</files>
  <action>
**Step 0 -- re-verify the call-site claim fresh (do this before editing anything):** run a grep for `ask_blocking_question` across `agents/`, `workflows/`, and `commands/`. Confirm it appears ONLY in `agents/gsd-phase-coordinator.md` (as of planning time: 2 matches in that one file -- a mention in prose and the actual call). If a NEW call site has appeared elsewhere since 2026-07-04, apply the same fallback wrapping there too and note this in the SUMMARY as a deviation from the plan's assumption. If the grep confirms zero matches elsewhere (expected), document the exact grep command and its output in the SUMMARY as the verified negative finding for `workflows/execute-roadmap.md` (it has fire-and-forget `send_message`/`send_status_update` notifications only -- nothing to wrap).

**`.planning/config.json`:** the file already has a `"telegram": { "polling_interval_minutes": 30, "long_poll_timeout_seconds": 60, "session_log_retention_days": 90 }` block. Add a new key to that SAME block: `"escalation_timeout_minutes": 30`. Do not create a new top-level section -- this belongs with the other telegram-specific tunables.

**`agents/gsd-phase-coordinator.md`:** locate the `**Escalate sensitive items via Telegram (multi-turn loop with JSONL logging):**` section (the per-`sensitive_items` loop containing Step A/B/C). Make these changes:

1. Immediately before the loop's `Initialize loop state:` line, add a step to read the configurable timeout: `Read the escalation timeout from \`.planning/config.json\`'s \`telegram.escalation_timeout_minutes\` field (default: 30 if the file is missing, unparseable, or the key is absent). Use this value as \`escalation_timeout_minutes\` for every \`ask_blocking_question\` call in this loop (replacing the previously hardcoded \`timeout_minutes: 30\`).`

2. In **Step A**, replace the hardcoded `timeout_minutes: 30` in the `ask_blocking_question` call with `timeout_minutes: {escalation_timeout_minutes}`.

3. Immediately after Step A's `ask_blocking_question` call block, add a new **Step A-fallback** subsection: `**Step A-fallback — daemon-down or timeout during escalation:** If the \`ask_blocking_question\` call in Step A throws or returns an error (IPC timeout, a \`DAEMON_UNAVAILABLE\`-coded error, or the daemon's own question-timeout bubbling back as an IPC error response) -- covers BOTH "user didn't reply in time" and "daemon was down at escalation time," same handling for both, do not build two paths:`
   ```
   1. Log the failure loudly (this failure must be visible in the milestone audit):
      "Telegram escalation failed for phase {phase_number}, gray_area: {item.gray_area} — {error message}. Applying timeout-fallback: writing DEFERRED.json waiver, parking to Claude's Discretion, continuing."

   2. Write a DEFERRED.json waiver via the ONLY sanctioned mechanism (do not invent a parallel one):
      node get-shit-done/bin/gsd-tools.js deferred add {phase_number} --step discuss --reason "Telegram escalation timed out/unavailable for: {item.question}" --approver timeout-fallback

   3. Append a JSON line to .planning/telegram-sessions/{YYYY-MM-DD}.jsonl:
      {"type":"escalation_complete","timestamp":"{ISO}","phase":{phase_number},"gray_area":"{item.gray_area}","final_answer":null,"turns_used":{turn},"escalated_to_discretion":true,"fallback_reason":"{error message}"}

   4. Push item into discretion_items (NOT escalated_answers) so CONTEXT.md's Claude's Discretion subsection still documents it:
      discretion_items.push({ gray_area: item.gray_area, question: item.question, reason: "Telegram escalation timeout/unavailable — parked via timeout-fallback" })

   5. Exit this item's loop immediately (do not attempt Step B/C for this item) and continue to the next item in sensitive_items.
   ```

4. Confirm the merge step (`Append escalated_answers to sufficient_answers before writing CONTEXT.md`) and the discretion-items handling remain otherwise unchanged -- items that hit the fallback simply flow into `discretion_items` like any non-sensitive discretion item, per existing Phase 22 behavior.
  </action>
  <verify>Read the edited `agents/gsd-phase-coordinator.md` back and confirm: the hardcoded `timeout_minutes: 30` in Step A is replaced with the config-driven variable; a Step A-fallback subsection exists immediately after Step A covering both timeout and daemon-down cases with one shared path; the exact `deferred add ... --approver timeout-fallback` command matches `cmdDeferredAdd`'s real CLI signature (positional phase, `--step`, `--reason`, `--approver`). Read `.planning/config.json` back and confirm `telegram.escalation_timeout_minutes: 30` is present inside the existing `telegram` block.</verify>
  <done>Step A reads a configurable timeout (default 30) instead of a hardcoded literal; a fallback path exists that logs loudly, writes a DEFERRED.json waiver with approver timeout-fallback via the sanctioned CLI, parks the item to Claude's Discretion, and continues the loop -- covering both timeout and daemon-down-at-escalation-time with one shared path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test proving the timeout-fallback CLI invocation actually works</name>
  <files>get-shit-done/bin/gsd-tools.test.js</files>
  <behavior>
    - The exact CLI shape the coordinator prompt now invokes (`deferred add {phase} --step discuss --reason "..." --approver timeout-fallback`) writes a DEFERRED.json entry with `approver: 'timeout-fallback'`, `step: 'discuss'`, and the given reason -- assert the full entry shape (all 6 schema fields present, matching the existing `deferred add` test conventions already in this file).
    - `deferred list {phase}` immediately surfaces that entry (round-trip proof: write via `deferred add`, read back via `deferred list`, assert the `timeout-fallback` entry appears in the returned `waivers` array).
    - A SECOND fallback entry for a different gray_area on the same phase appends rather than overwrites (matches the existing accumulation regression test pattern already in this file for `deferred add`) -- confirms multiple escalation timeouts in one phase run each get their own waiver, not a clobber.
    - The `--plan` flag is optional for this use case (the coordinator's escalation loop runs at the `discuss` step, before any plan exists yet) -- confirm `deferred add {phase} --step discuss --reason "..." --approver timeout-fallback` (no `--plan` flag) succeeds and the resulting entry has `plan: null`.
  </behavior>
  <action>
Locate the existing `describe('deferred add/list commands (Phase 45-03)', ...)` block in `get-shit-done/bin/gsd-tools.test.js` (uses `createTempProject()`, `runDeferred()` helpers already defined there). Add a NEW sibling `describe('timeout-fallback waiver wiring (Phase 47-03)', ...)` block reusing the same `createTempProject()`/`runDeferred()` helpers (do not redefine them -- they are already in scope in this file). Cover every behavior listed above.

Use only the existing `createTempProject()` temp-directory helper -- never write to this repo's own real `.planning/phases/47-telegram-reliability/DEFERRED.json`.

If the Agent tool is available, spawn `gsd-test-writer` for this addition; otherwise write it directly (documented fallback).

Run: `cd /Users/ollorin/get-shit-done && npm test` and confirm the new describe block's tests pass alongside the existing 251.
  </action>
  <verify>`npm test` from repo root exits 0, count is 251 + (number of new tests added here), 0 failures.</verify>
  <done>A new describe block in gsd-tools.test.js proves the exact `deferred add ... --approver timeout-fallback` invocation the coordinator prompt now runs actually writes and surfaces a waiver via `deferred list`, all green.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification -- fresh call-site grep + full test-suite green</name>
  <files>N/A (verification-only task -- runs existing test suites, creates/modifies no files)</files>
  <action>
1. Re-run the `ask_blocking_question` grep across `agents/`, `workflows/`, `commands/` one more time (fresh, same-session) and confirm the result matches what Task 1 documented -- only `agents/gsd-phase-coordinator.md`. Record the exact command and output in the SUMMARY.
2. Run `cd /Users/ollorin/get-shit-done && npm test` -- confirm exit 0, pass count >= 251 + Task 2's new tests, 0 failures.
3. Read `agents/gsd-phase-coordinator.md` back once more end-to-end around the escalation loop section and confirm the loop still terminates for every `sensitive_items` entry regardless of outcome (sufficient answer, discretion-fallback after max_turns, OR the new timeout/daemon-down fallback) -- no code path leaves an item unresolved.
4. Confirm `.planning/config.json`'s `telegram.escalation_timeout_minutes` key does not collide with or duplicate any existing key in that block.
  </action>
  <verify>Fresh grep output matches Task 1's documented finding; `npm test` exits 0 with the expected count; the escalation loop has no code path that leaves an item permanently unresolved.</verify>
  <done>Call-site claim re-confirmed fresh; full test suite green; every sensitive_items entry is guaranteed to resolve (answered, discretion after max_turns, or timeout-fallback) -- none can hang the phase run indefinitely.</done>
</task>

</tasks>

<verification>
- Fresh grep for `ask_blocking_question` across `agents/`, `workflows/`, `commands/` returns matches only in `agents/gsd-phase-coordinator.md`.
- `npm test` from repo root exits 0, count >= 251 + new tests, 0 failures.
- `agents/gsd-phase-coordinator.md`'s Step A no longer hardcodes `timeout_minutes: 30`; a Step A-fallback subsection exists.
- `.planning/config.json`'s `telegram` block contains `escalation_timeout_minutes`.
</verification>

<success_criteria>
A blocking Telegram question that times out, or fails because the daemon is down at escalation time, resolves to a defined fallback (DEFERRED.json waiver with approver `timeout-fallback`, item parked to Claude's Discretion, run continues) instead of hanging the phase indefinitely. The escalation timeout is configurable. The exact CLI invocation the fallback prose depends on is proven end-to-end by a real, passing test.
</success_criteria>

<output>
After completion, create `.planning/phases/47-telegram-reliability/47-03-SUMMARY.md`
</output>
