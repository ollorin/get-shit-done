# Deferred Items — Phase 55 (Failures-to-Regression Pipeline)

- [RESOLVED 2026-07-06 by 55-03] `.planning/REQUIREMENTS.md` marks MILE-32 as `[x]` Complete as of commit `447ca4c` (55-01's final metadata commit), but MILE-32's full text requires "accepted candidates become permanent eval cases run by the existing eval harness in CI" — that CI-wiring work is Plan 55-03's scope and has not landed yet as of 55-02. This premature marking predates Plan 55-02 (found during 55-02's execution, not introduced by it) and is out of scope for 55-02 to fix per the scope-boundary rule (only fix what you break). Recommend Plan 55-03's executor either confirm the checkbox is still accurate once 55-03 lands, or a human reconciles the checkbox state now if Phase 55 is not actually expected to fully close CI-execution scope under MILE-32.
  - **Resolution:** Plan 55-03 landed the CI-execution half (loader/executor in eval-harness.js, `eval regress` CLI, npm test chain + dedicated eval-harness.yml eval-regress job) and all 4 MILE-32 integration scenarios pass end-to-end via the real CLI (595/595 npm test). The `[x]` checkbox and the "MILE-32 | Phase 55 | Complete" mapping row in REQUIREMENTS.md are now accurate — no reconciliation needed.
